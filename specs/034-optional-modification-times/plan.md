# Implementation Plan

Add a comparison option propagated from the five eligible CLI commands into classification. Preserve it in mutation plans so execution, revalidation, and metadata application use the same policy. Cover default no-op and short/long opt-in transfer behavior.
